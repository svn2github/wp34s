This directory contains programs that generate the various constants
used by the calculator.  These constants should be manually inserted
into ../compile_consts.c which will then be built by a make in the
parent directory.
