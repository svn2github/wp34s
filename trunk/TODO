Complex Bessel functions of complex non-integer order doesn't seem correct.

six character arg commands -- one char overflow it seems.

proper display of numbers (as a single step)
proper display of alpha (as a single step)

test [alpha]' and implement #'

LBL B, C, D -> LBL'B', ...

Complex igamma, complex error function.

Date Julian/Gregorian change over.
	The suggestion is 1582-10-15 is 2299161, while the day
	before was 1582-10-04 with JD 2299160 (last day of the
	Julian calendar).  Currently we use the British version.

